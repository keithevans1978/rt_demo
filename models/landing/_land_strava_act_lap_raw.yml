version: 2

models:
  - name: land_strava_act_lap_raw  # <-- Replace with your actual model name (e.g. strava_activity_lap)
    description: "Processed view of Strava activity laps with surrogate key."
    columns:
      - name: ACTIVITY_LAP_ID
        description: "Surrogate key generated from ACTIVITY_ID and LAP_NUMBER"
        tests:
          - unique
          - not_null