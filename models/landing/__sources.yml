version: 2

sources:
    - name: KANSAS_BASKETBALL
      description: "This is the blog data base"
      database: KANSAS_BASKETBALL
      schema: PUBLIC
      tables:
        - name: VW_TEAM
          description: 'This is the table of the garmin machines'
          columns:
          - name: team_name
            description: "the primary key for this table"
            tests:
                - unique
                - not_null

          